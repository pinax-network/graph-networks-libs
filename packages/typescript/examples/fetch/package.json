{
  "name": "fetch-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsc --noEmit *.ts && esbuild --bundle index.ts | node"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "esbuild": "^0.25.5",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@pinax/graph-networks-registry": "file:../.."
  }
}
