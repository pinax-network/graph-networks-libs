{
    "name": "local-example",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "start": "tsc --noEmit *.ts && esbuild --bundle --format=esm --platform=node --packages=external index.ts | node --input-type=module"
    },
    "devDependencies": {
        "@types/node": "^24.0.10",
        "esbuild": "^0.25.5",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@pinax/graph-networks-registry": "file:../../"
    }
}
